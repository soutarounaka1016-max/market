# AGENTS.md

## 1. このファイルの目的

このファイルは、Codexがこのリポジトリで作業するときに守る共通ルールを定める。

アプリの詳しい仕様は `PRODUCT_SPEC.md`、現在の進捗は `PROGRESS.md`、利用方法は `README.md` を参照すること。

ユーザーに細かな作業手順を毎回質問するのではなく、既存資料とコードを確認し、合理的な範囲で自律的に判断して作業すること。

---

## 2. 作業開始時の手順

作業を始める前に、必ず次の順番で確認する。

1. この `AGENTS.md`
2. `PRODUCT_SPEC.md`
3. `PROGRESS.md`
4. `README.md`
5. リポジトリのファイル構成
6. 関連する既存コード
7. 利用可能なテストや確認方法

資料と実際のコードが食い違っている場合は、実際の動作を確認する。

コードを確認せずに、推測だけで修正しないこと。

---

## 3. 利用者と開発環境

主な利用者は、高校生の開発者本人。

主な利用環境は次のとおり。

* 学校支給iPad
* Safari
* GitHub
* GitHub Pages
* ブラウザ中心
* アプリのインストール不可

専門用語を必要以上に増やさず、開発者本人が後から理解できる構成を優先すること。

---

## 4. 基本方針

次の順番で優先する。

1. アプリの中心価値
2. 正常に動作すること
3. データを失わないこと
4. iPadのSafariで使いやすいこと
5. コードの分かりやすさ
6. 見た目
7. 装飾や追加演出

見た目だけの改善を、主要機能やバグ修正より優先しないこと。

一度に多くの機能を追加するより、確認可能な小さな単位で確実に進めること。

---

## 5. 自律的な作業判断

依頼内容に細かな実装手順がない場合は、次の流れで進める。

1. 現状を調査する
2. 問題または未完成部分を特定する
3. 今回の作業範囲を決める
4. 必要な変更を実装する
5. テストする
6. 問題があれば修正する
7. 関連資料を更新する
8. 作業結果を報告する

軽微な判断については、ユーザーへの質問を増やさず、次の条件を満たす方法を選ぶ。

* 単純
* 安全
* 元に戻しやすい
* 既存の設計と一貫している
* iPadのSafariで動かしやすい
* 将来の変更を妨げない

---

## 6. ユーザーへの確認が必要な場合

次の場合は、勝手に進めず確認するか、作業を停止して明確に報告する。

* 保存済みデータを失う可能性がある
* mainブランチの履歴を大きく変更する
* 外部の有料サービスを導入する
* APIキーや秘密情報が必要になる
* 個人情報を新しく収集する
* 大きな仕様変更になる
* 選択肢によって利用者体験が大きく変わる
* 既存の主要機能を削除する
* 利用規約や法律上の問題が疑われる

軽微な色、余白、変数名、内部構造などについては、原則として確認せず合理的に判断する。

---

## 7. 実装ルール

* 既存の正常な機能を壊さない
* 変更前に関連コードを確認する
* 同じ機能を重複して実装しない
* 不要なファイルや依存関係を増やさない
* 現在の技術構成を尊重する
* 過度に複雑な設計を避ける
* 将来使うかもしれないという理由だけで機能を追加しない
* エラーを握りつぶさない
* ユーザーに表示するエラーは分かりやすい日本語にする
* APIキーやパスワードをコードへ直接記載しない
* 秘密情報をGitHubへコミットしない
* コメントは必要な理由を説明するときに使う
* 自明な処理をコメントで繰り返さない

---

## 8. UI・操作ルール

* 日本語を基本とする
* iPadのSafariを最優先で考える
* スマートフォンとタブレットの両方で崩れにくくする
* ボタンや入力欄は押しやすい大きさにする
* 重要な操作を分かりやすく表示する
* 削除など取り消しにくい操作には確認を入れる
* 保存成功や失敗を画面上で伝える
* 横スクロールをできるだけ発生させない
* 装飾より可読性を優先する
* 派手なアニメーションを勝手に追加しない

---

## 9. データ管理ルール

* 保存形式を変更する場合は、既存データとの互換性を考える
* 保存済みデータが消える変更を避ける
* データ構造を変更するときは移行方法を用意する
* 不正または不足したデータでも、可能な範囲で安全に表示する
* 削除操作の前には確認する
* インポートするデータは形式を検証する
* データ破損時にアプリ全体が停止しないようにする

---

## 10. テストと確認

変更後は、変更内容に応じて利用可能な確認を行う。

最低限、次を確認する。

* 構文エラーがない
* 対象機能が期待どおり動く
* 保存後に再読み込みしてもデータが残る
* 追加、編集、削除が正常に動く
* 空欄や不正入力で致命的なエラーにならない
* 既存の主要機能が壊れていない
* iPad相当の画面幅で大きく崩れない

テストコマンドが存在する場合は実行する。

テストを実行できない場合は、実行したと偽らず、理由と人間が確認すべき操作を報告する。

テスト失敗を放置したまま、すべて完了したと報告しない。

---

## 11. バグ修正の手順

バグを修正するときは、次の順番で進める。

1. 症状を確認する
2. 再現条件を特定する
3. 原因を調査する
4. 必要最小限の修正を行う
5. 同じバグが再発しないことを確認する
6. 関連機能への影響を確認する

原因が不明なまま、関係しそうなコードを大量に書き換えないこと。

---

## 12. 仕様書と進捗の更新

機能、仕様、データ構造、利用方法を変更した場合は、必要に応じて関連資料を更新する。

### PRODUCT_SPEC.md

次の場合に更新する。

* 正式な機能を追加または削除した
* アプリの目的や対象者が変わった
* 重要な仕様判断を行った
* 「現時点では作らないもの」が変わった

### PROGRESS.md

各作業の完了時に更新する。

最低限、次を記録する。

* 今回完了したこと
* 現在動作している機能
* 未完成の項目
* 判明している問題
* 次に優先すべき作業
* 最終更新日

### README.md

起動方法、公開方法、操作方法が変わった場合に更新する。

資料だけを更新して、存在しない機能を完成済みとして記録しないこと。

---

## 13. Gitの変更方針

* 関係のないファイルを変更しない
* 自動整形だけによる巨大な差分を避ける
* 変更理由が分かる単位にまとめる
* 既存の変更を勝手に取り消さない
* ユーザーが行った未完成の変更を尊重する
* mainへの危険な直接変更より、確認可能な変更方法を優先する
* 履歴の強制書き換えを行わない

---

## 14. 禁止事項

次の行動をしてはいけない。

* 依頼されていない巨大機能を勝手に追加する
* アプリ全体を理由なく作り直す
* 正常な実装を好みだけで別方式に置き換える
* 外部ライブラリを無断で大量に追加する
* 有料APIを無断で導入する
* APIキーを公開コードに含める
* 保存済みデータを無断で削除する
* テスト未実施なのに「すべて確認済み」と報告する
* 仮データを実際の最新データとして扱う
* 見た目のために中心機能を複雑化する
* 資料とコードの不一致を放置する

---

## 15. 作業完了時の報告形式

作業終了時は、次の形式で簡潔に報告する。

### 実施内容

何を変更したか。

### 変更ファイル

どのファイルを変更したか。

### テスト結果

何を実行し、何が成功または失敗したか。

### 人間による確認

iPadや公開環境など、人間が確認する必要があること。

### 残っている問題

未解決のバグや制限。

### 次の優先作業

次に進めるべき作業を1つから3つ。

---

## 16. 最終原則

動くことを、賢そうに見えることより優先する。

利用者に価値があることを、機能数より優先する。

確認できた事実と推測を区別する。

不明点があっても軽微なものなら安全な方法で前進し、重大な判断だけをユーザーに委ねる。
