# 社会ニーズ発見レーダー 開発進捗

最終更新日：2026年7月19日
現在の状態：GitHub IssuesとHacker Newsを共通の外部課題候補として取得・表示・個別保存できる状態

## 1. 現在の位置付け

このプロジェクトの長期ビジョンは、AIが合法的かつ技術的に利用可能な公開情報を調査・分析し、社会の未充足ニーズや非効率を収益化可能な事業機会として整理する「AI市場調査・事業機会発見エージェント」を作ることである。

現在は、手動で課題を登録・保存するフェーズ1の土台を保ちながら、フェーズ2またはフェーズ3へ向けた実験として、外部APIから取得した公開情報を保存前の課題候補として表示し、利用者が確認したものだけ既存の課題一覧へ保存できる段階である。

現在のmainブランチのコードを唯一の事実として扱う。過去のPull Request説明で実装済みとされていた事業性評価入力・総合点計算・評価順並べ替えは、現在のアプリ本体には存在しないため未実装として扱う。

## 2. 今回完了したこと

* 現在の `index.html`、`styles.css`、`app.js`、管理ファイルを確認し、GitHub Issues取り込みに未定義変数・未定義関数・重複submit処理が混在していた問題を修正した。
* GitHub Issues取得を、保存前候補の一覧表示と1件ずつの保存方式へ統一した。
* 古い「取得したIssueを即時一括保存する方式」を削除した。
* GitHub Issues APIのレスポンスからPull Requestを除外する処理を維持した。
* GitHub Issueの情報源名と情報源URLを分けて保存するように整理した。
* 外部情報源を追加しやすい共通課題候補モデルを追加した。
* Hacker News Search API（Algolia）から投稿を取得し、GitHub Issuesと同じ候補一覧・保存処理で扱えるようにした。
* 候補の重複保存を、情報源URLまたは情報源種別と外部データIDで判定するようにした。
* 管理ファイルを現在のコードと一致する内容へ更新した。

## 3. 現在動作している機能

* 課題のタイトル入力
* 課題の詳しい内容の入力
* 困っている人や企業の入力
* 支払者候補の入力
* 情報源の入力
* 課題の登録
* 登録した課題の一覧表示
* 登録済み課題の編集
* 登録済み課題の削除
* 削除前の確認表示
* `localStorage` への保存
* ページ再読み込み後のデータ復元
* 壊れた保存データを読み込んでもアプリ全体を停止させない処理
* タイトル未入力時の日本語エラー表示
* スマートフォン・iPad相当の幅で1カラムに切り替わる表示
* 公開GitHubリポジトリの未完了Issue取得
* GitHub Issues APIレスポンス内のPull Request除外
* Hacker News投稿の検索取得
* 外部情報を共通課題候補へ変換
* 保存前の課題候補一覧表示
* 候補の元ページを新しいタブで開くリンク表示
* 候補を1件ずつ課題として保存
* 保存済み候補の再保存防止

## 4. 未完成の機能

* iPad Safari実機での操作確認
* GitHub Pages公開環境での実API動作確認
* 事業性評価入力、総合点計算、評価順並べ替え
* キーワード検索
* 業界による絞り込み
* 検証状態の管理
* 課題の詳細表示
* 事業仮説の入力
* JSON形式での書き出し
* JSON形式での読み込み

## 5. 判明している問題と制限

* iPadのSafariでの実機確認はまだできていない。
* GitHub Pagesの公開設定はまだ確認していない。
* JSON書き出し・読み込みはまだないため、ブラウザ外へのバックアップ手段がない。
* 現在の保存データは基本項目と外部情報源の参照情報が中心で、評価項目、業界、検証状態、事業仮説はまだ保存していない。
* GitHub Issues APIは公開リポジトリの未完了Issueのみを対象にしている。
* 認証なしのGitHub API利用なので、GitHub側のレート制限を受ける可能性がある。
* Hacker News検索は検索語に合う投稿を返すため、社会課題と関係の薄い投稿も候補に含まれる可能性がある。
* この作業環境ではDNS解決が一時的に失敗し、実APIへのライブ取得確認は完了できなかった。モックデータによる変換・保存処理は確認済み。
* AIによるURL解析、自動収集、ランキング、定期調査は未実装であり、完成済みとして扱わない。

## 6. 現時点での仕様判断

### データ保存

MVPではブラウザの `localStorage` を使用する。現在の保存キーは `marketNeeds.v1`。

保存している主な項目は、ID、タイトル、詳しい内容、困っている人や企業、支払者候補、情報源名、情報源URL、情報源種別、外部データID、取得日時、登録日、更新日。

GitHub IssuesとHacker Newsから保存した課題も同じ保存形式で保存する。APIだけでは判断できない困っている人、支払者候補、評価点などは勝手に作らない。

### 外部課題候補

外部APIレスポンスは、保存前に次の共通課題候補形式へ変換する。

```javascript
{
  candidateId,
  sourceType,
  sourceName,
  sourceUrl,
  externalId,
  title,
  description,
  author,
  tags,
  commentCount,
  score,
  publishedAt,
  updatedAt,
  fetchedAt,
  providerName,
  metadata
}
```

候補一覧と保存処理は、GitHub IssuesとHacker Newsで共通化する。

### GitHub Issues API連携

公開リポジトリの `owner/repo` またはGitHub URLを入力し、GitHub Issues APIから未完了Issueを最大30件取得する。

Pull RequestはIssue一覧APIにも含まれるため、取り込み対象から除外する。

### Hacker News連携

Hacker News Search API（Algolia）へ検索語を送り、新しい投稿を最大20件取得する。空欄の場合は既定の検索語で取得する。

### AI連携

現時点ではAI APIを直接接続しない。

フェーズ2以降で、URLや文章から課題候補を抽出する機能を検討する。

## 7. テスト結果

今回実行した確認は次のとおり。

* `node --check app.js`：JavaScriptの構文エラーがないことを確認した。
* `git diff --check`：差分の空白エラーがないことを確認した。
* `node - <<'NODE' ... NODE`：GitHubリポジトリ入力の解析、GitHub Issues APIレスポンスの候補変換、Pull Request除外、Hacker Newsレスポンスの候補変換、候補から課題への保存形式、重複判定、localStorage保存・読み込み、文字列切り詰めを確認した。
* `node - <<'NODE' ... NODE` による実APIライブ取得確認は、作業環境のDNS解決失敗（`EAI_AGAIN api.github.com`）により完了できなかった。

## 8. 人間による確認が必要なこと

* iPadのSafariでの登録、編集、削除、外部候補取得、候補保存の操作感。
* GitHub Pages上で正常に表示されるか。
* GitHub APIとHacker News APIの実API取得が公開環境で正常に動くか。
* API制限時や入力間違い時のエラー表示が分かりやすいか。
* 外部候補の本文表示量や検索語の初期値が実用上ちょうどよいか。

## 9. 次の優先作業

1. iPadのSafariまたは同等幅のブラウザで、外部候補取得と既存CRUD操作を手動確認する。
2. JSON書き出し・読み込みを追加し、ブラウザ外へバックアップできるようにする。
3. 後回しにした事業性評価機能を、現在のmainブランチを基準に安全に実装する。

## 10. 更新ルール

Codexは作業完了時に、このファイルを実際の状態に合わせて更新する。

コードを実装していない機能を完成済みとして記録しないこと。テストしていない内容を確認済みとして記録しないこと。
