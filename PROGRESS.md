# 社会ニーズ発見レーダー 開発進捗

最終更新日：2026年7月18日
現在の状態：フェーズ2・3に向けた外部API取得の初期実験を実装

## 1. 現在の位置付け

このプロジェクトの長期ビジョンは、AIが合法的かつ技術的に利用可能な公開情報を調査・分析し、社会の未充足ニーズや非効率を収益化可能な事業機会として整理する「AI市場調査・事業機会発見エージェント」を作ることである。

現在は、手動で課題を登録・保存するフェーズ1の土台を保ちながら、フェーズ2またはフェーズ3へ向けた最初の実験として、GitHubの公開Issueを外部APIから取得し、保存前に内容を確認してから既存の課題一覧へ保存できる段階に進んでいる。

現在のコードを確認した結果、過去のPull Request説明で実装済みとされていた事業性評価入力・総合点計算・評価順並べ替えは、現在のアプリ本体には存在しない。現在のmainブランチ相当のコードを唯一の事実として、評価機能は未実装または失われた機能として扱う。既存データに評価情報が含まれている場合は、読み込み時に削除しないよう互換性を保つ。

## 2. 現在動作している機能

### 手動登録と保存

* 課題のタイトル入力
* 課題の詳しい内容の入力
* 困っている人や企業の入力
* 支払者候補の入力
* 情報源の入力
* 課題の登録
* 登録した課題の一覧表示
* 登録済み課題の編集
* 登録済み課題の削除
* 削除前の確認表示
* `localStorage` への保存
* ページ再読み込み後のデータ復元
* 壊れた保存データを読み込んでもアプリ全体を停止させない処理
* 古いデータや追加項目を持つデータを、可能な範囲で保持したまま読み込む処理

### GitHub Issues API連携

* GitHubのユーザー名または組織名、リポジトリ名、Issue状態、取得件数を入力できる
* `https://api.github.com/repos/{owner}/{repo}/issues` から公開Issueを取得できる
* 取得件数は10件または20件から選択できる
* Issue状態は `open` または `all` から選択できる
* API通信中の表示と取得ボタンの無効化ができる
* 取得完了または失敗後に取得ボタンを再び使える
* Pull Requestを取得結果から除外できる
* 取得したIssueを保存前の課題候補としてカード表示できる
* 候補カードにタイトル、本文の一部、リポジトリ名、Issue番号、ラベル、コメント数、作成日、更新日、GitHub上の元ページリンク、保存ボタンを表示できる
* 元ページリンクは新しいタブで開き、`rel="noopener noreferrer"` を付けている
* 候補を1件ずつ既存課題へ保存できる
* 保存済み候補はボタンが「保存済み」になり、同じIssueの重複保存を防止できる
* 外部データIDまたは情報源URLで重複判定できる
* API残り利用回数をレスポンスヘッダーから取得できた場合は表示できる

## 3. 今回完了したこと

* 管理ファイルと現在のアプリ本体を確認し、過去PR説明ではなく現在のコードを事実として進捗を整理した
* 以前の「取得したIssueを即保存する」動作をやめ、保存前の課題候補として確認できる画面に変更した
* GitHub取得フォームを、ユーザー名または組織名、リポジトリ名、Issue状態、取得件数に分けた
* GitHub REST APIからIssue一覧を取得する処理を追加した
* Pull Request除外、Issue候補への変換、候補表示、1件ずつの保存、重複保存防止を実装した
* GitHub由来の課題に、情報源、情報源URL、外部データID、取得日時を保存するようにした
* 古い `localStorage` データや評価情報を含む既存データを失わないよう、読み込みと手動編集時の互換性を改善した
* README、PRODUCT_SPEC、PROGRESSを現在の実装に合わせて更新した

## 4. 未完成の機能

* iPad Safari実機での操作確認
* GitHub Pages公開環境での実API動作確認
* 事業性評価入力、総合点計算、評価順並べ替えの再実装
* キーワード検索
* 業界による絞り込み
* 検証状態の管理
* JSON形式での書き出しと読み込み
* AIによる要約、事業性判断、ランキング

## 5. 判明している問題と制限

* Codex環境では外部GitHub APIへの直接通信がプロキシにより403で遮断されたため、実API疎通はmock fetch中心の自動テストにとどまる
* 認証トークンを使わないため、GitHub APIの未認証レート制限を受ける可能性がある
* 非公開リポジトリは取得できない
* 取得したIssue本文は候補一覧では省略表示するが、保存時は本文全体を課題の詳しい内容として保存する
* APIだけでは困っている人、支払者候補、評価点を判断できないため、GitHub由来の課題では空欄または未評価のまま保存する
* 現在のアプリ本体には評価入力UIがないため、評価機能は完成済みとして扱わない

## 6. テスト結果

今回実行した確認は次のとおり。

* `node --check app.js`：JavaScriptの構文エラーがないことを確認した
* `node - <<'NODE' ... NODE`：手動課題の作成・保存・読み込み、既存追加項目と評価データの保持、GitHub入力検証、API URL生成、Pull Request除外、長い本文と空本文の候補化、候補から課題への保存変換、重複判定、mock fetchでの正常取得・404・403・ネットワーク失敗・0件取得を確認した
* `git diff --check`：差分の空白エラーがないことを確認した
* `rg '^(<<<<<<<|=======|>>>>>>>)' . -g '!node_modules'`：コンフリクト記号が残っていないことを確認した
* `curl -I -L --max-time 10 'https://api.github.com/repos/octocat/Hello-World/issues?state=open&per_page=1'`：Codex環境のプロキシで403になり、実API疎通は確認できなかった

## 7. 人間による確認が必要なこと

* iPad Safariで、手動登録、編集、削除、再読み込み後の保存維持を確認する
* iPad Safariで、GitHub Issue取得、候補表示、元ページリンク、1件ずつ保存、重複保存防止を確認する
* GitHub Pages公開環境で、公開リポジトリを指定した実API取得を確認する
* 存在しないリポジトリ、Issueが0件の条件、API制限時のエラー表示が分かりやすいか確認する

## 8. 次の優先作業

1. iPad SafariまたはGitHub Pages上で、実GitHub APIを使った手動確認を行う
2. JSON書き出し・読み込みを追加し、ブラウザ外へバックアップできるようにする
3. 現在のmainブランチを基準に、事業性評価入力・総合点計算・並べ替えを安全に再実装する

## 9. 更新ルール

Codexは作業完了時に、このファイルを実際の状態に合わせて更新する。

コードを実装していない機能を完成済みとして記録しないこと。テストしていない内容を確認済みとして記録しないこと。
